#include<stdio.h>
#include<conio.h>
#include<string.h>
void main()
{
int len,i,j;
char str[30];
clrscr();
printf("Enter the String: \n");
scanf("%s",&str);
len = strlen(str);
i = (len/2);
j=i;
if(j<=len){
  printf("%c",str[i]);
  j++;
}

getch();
}
